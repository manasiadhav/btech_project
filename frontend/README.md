# Frontend (React + Vite)

Quick start (Windows PowerShell):

1. Install dependencies:

```powershell
cd frontend
npm install
```

2. Start dev server:

```powershell
npm run dev
```

By default the app expects the backend at http://localhost:5000. To change, set VITE_API_BASE in an .env file.
